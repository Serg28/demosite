<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Калькулятор кошика
    |--------------------------------------------------------------------------
    | BasketCalculator: baseAmount = price×qty, totalAmount = baseAmount - discount,
    | subtotal = totalAmount. Без ПДВ (tax = 0).
    */
    'calculator' => \App\Services\Cart\Calculators\BasketCalculator::class,

    /*
    |--------------------------------------------------------------------------
    | Податок (ПДВ)
    |--------------------------------------------------------------------------
    | 0 — ціни вже включають ПДВ, окремо не нараховуємо.
    */
    'tax' => 0,

    /*
    |--------------------------------------------------------------------------
    | Налаштування бази даних
    |--------------------------------------------------------------------------
    */
    'database' => [
        'connection' => null,
        'table'      => 'shoppingcart',
    ],

    /*
    |--------------------------------------------------------------------------
    | Очищення кошика при виході
    |--------------------------------------------------------------------------
    */
    'destroy_on_logout' => false,

    /*
    |--------------------------------------------------------------------------
    | Формат чисел
    |--------------------------------------------------------------------------
    */
    'format' => [
        'decimals'           => 0,
        'decimal_point'      => '.',
        'thousand_separator' => '',
    ],

    /*
    |--------------------------------------------------------------------------
    | Екземпляри кошика
    |--------------------------------------------------------------------------
    | Розширюй тут для нових бізнес-сценаріїв (wishlists, quotes, etc.)
    */
    'instances' => [
        'default'  => 'default',
        'wishlist' => 'wishlist',
    ],

    /*
    |--------------------------------------------------------------------------
    | Pipeline pipes для операції "add"
    |--------------------------------------------------------------------------
    | Порядок важливий: валідація → перевірка наявності → виконання.
    | Додавай кастомні pipes (знижки, бандли, тощо) у цей список.
    */
    'pipes' => [
        'add' => [
            \App\Services\Cart\Pipes\ValidateProductPipe::class,
            \App\Services\Cart\Pipes\CheckAvailabilityPipe::class,
            \App\Services\Cart\Pipes\PerformAddPipe::class,
        ],
    ],

];

<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Включение/выключение прероутера
    |--------------------------------------------------------------------------
    */
    'enabled' => env('PREROUTER_ENABLED', false),

    /*
    |--------------------------------------------------------------------------
    | Автоматическая перегенерация кеша при cache:clear
    |--------------------------------------------------------------------------
    */
    'auto_rebuild_on_cache_clear' => env('PREROUTER_AUTO_REBUILD', true),

    /*
    |--------------------------------------------------------------------------
    | Класс для получения шаблонов Tree
    |--------------------------------------------------------------------------
    */
    'tree_templates_class' => \App\Cms\Tree\Tree::class,

    /*
    |--------------------------------------------------------------------------
    | Исключения - префиксы URL, которые не должны обрабатываться
    |--------------------------------------------------------------------------
    */
    'exclude_prefixes' => [
        'admin',
        'api',
        'livewire',
        'horizon',
        'telescope',
        'auth',
        'storage',
        'sanctum',
        '_debugbar',
    ],

    /*
    |--------------------------------------------------------------------------
    | Источники данных для прероутинга
    |--------------------------------------------------------------------------
    | Включайте только те, которые действительно используются
    */
    'sources' => [
        'tree'     => true,   // Страницы сайта
        'news'     => false,  // Новости (включить при необходимости)
        'category' => false,  // Категории каталога (включить при необходимости)
        'product'  => false,  // Товары (включить при необходимости)
    ],

    /*
    |--------------------------------------------------------------------------
    | Маппинг моделей
    |--------------------------------------------------------------------------
    */
    'models' => [
        'tree'     => \App\Models\Tree::class,
        'news'     => \App\Models\News::class,
        'category' => \App\Models\Category::class,
        'product'  => \App\Models\Product::class,
    ],

    /*
    |--------------------------------------------------------------------------
    | Маппинг контроллеров и методов
    |--------------------------------------------------------------------------
    | Формат: [ClassName::class, 'methodName']
    | Для Tree контроллер определяется динамически при построении кеша
    */
    'controllers' => [
        'tree'     => [\Linecore\Cms\ControllersNew\TreeController::class, 'init'], // Fallback
        'news'     => [\App\Http\Controllers\NewsController::class, 'show'],
        'category' => [\App\Http\Controllers\CategoryController::class, 'show'],
        'product'  => [\App\Http\Controllers\ProductController::class, 'show'],
    ],

];
